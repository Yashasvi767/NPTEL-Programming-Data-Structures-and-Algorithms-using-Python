# Strip Whitespace
```python
1. s.rstrip()
```
removes trailing whitespace

```python
2. s.lstrip()
```
removes leading whitespace

```python
3. s.strip()
```
removes leading and trailing whitespace

# Searching for text
